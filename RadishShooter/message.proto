syntax = "proto3";

package message;

enum Corpus {
    UPDATE = 0;
    SHOOT = 1;
  }

message vec3 {
    float x = 1;
    float y = 2;
    float z = 3;
}

message Message {
    Corpus corpus = 1;
    vec3 pos = 2;
}